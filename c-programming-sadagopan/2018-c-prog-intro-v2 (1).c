#include<stdio.h>

// stdio.h - standard input and output

void main()
{
int a,b,c;
a=3, b=5, c=a+b;

printf("the value of a=%d, b=%d, c=%d", a,b,c);


/*
Declaration

*/


/*

computation
*/

/*

display

*/

// modular programming

int Name, INt, FLOAT, Na_1, bxy1; 

// , $N^a$4fas

int v1, v2, v3;

// printf- output - interface system and the monitor

// scanf - input - interface system and the monitor via keyboard

printf(" \n \n please enter the values of v1 and v2 \n");

scanf("%d%d", &v1); // & : memory reference operator - this indicates the CPU must redirect the values of variables v1 and v2 in the space reserved by the declaration sts (space in RAM)

// & - if you miss & - segmentation fault(core dumped)
v3=v1*v2;

printf("the mul of v1=%d and v2=%d is v3=%d \n \n", v1,v2,v3);

printf("\n \n variable with && symbol - memory reference operator  v1=%d \n and v2=%d \n is v3=%d \n \n", &v1,&v2,&v3);

// & - memory ref - refers to memory address - integers -

// 32 bit system - least value , max value - 4 GB RAM - 2^2 * 10 ^9 * 2^3 

// 10^9 ~ 2 ^30  --  10 ^9 = 10^3 * 10^3 * 10^3

// 2^10 = 1024

// 2^35 bits 

// 1 GB RAM - 2^30 bytes --

// fix space (number of bits) for integers - int - 4 bytes - 4*8=32 bits

// using an integer variable 0000000 to 11111 -- in deciaml -- 0 to 2^32-1



// 1 byte = 8 bits
// 1 nibble = 4 bits
//
}
