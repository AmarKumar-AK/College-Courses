#include<stdio.h>
/*
gedit - a tool to open a text editor

gedit & - runs gedit in the background --  gedit & throws a number on execution which denotes the process ID

// how to add two numbers

// Natural Lang 

// high level lang (machine / man)

// grammar --- grammatically incorrect sts lead to ambiguity -- difficult to parse the st / interpret the st

// NLP (Grammar) = HLL (Syntax)

--Sample Program (A sequence of logically related instructions)

*/

/*

switch ON
press 2, 3 ,(operands) + (operator)   (Input)
press =  (Output)

Process that converts the input into a valid output is called - Program
*/

//void main() {  int a,b,c; c=a+b; }

void main()  // start

{

int a, b ; // int : keyword - the variables a and b are integers // 2,3, - integers (decimal) 17.11, 18.29 - float (decimal with fraction)
// ; - to inform the EOL (end of line) to the system

a=-4;
b=50;

//int int;

int INT; // case sensitive  keyword - int, float, printf  identifier - userdefined 

int d=410.1;
// input is complete

// process starts - addition

int c;  c=a+b;   // every variable must be declared before its use

//printf("the addition of a=%d  b=%d is c=%d  \n \n \n \n",a,b, c);  // %d - control string (format specifier)  refers to an integer


//printf("the addition of a=5 b=3 with the control string \%d is c=%d  \n \n \n \n",a,b, c);  // %d - control string (format

//printf("the addition of a=5 b=3 with the control string  '%' 'd' is c=%d  \n \n \n \n",a,b, c);  // %d -
// \n - escape sequence - introduces a new line - transfers the control to the next line

//printf("i am printing control string \%");
printf("i am printing control string %%");
printf("d adfkasdjfk %%d \n");


printf("the value of d is %d \n", d);


int x=5,y=3,z=x+y;

printf("\n \n the value of x=%d, y=%d, x+y=%d \n",x,y,z);


//int a1=b1=3;

//int a1=3;  //how does the compiler parses this st -- is it left to right or R L

//declaration: is L R
//assignment is R L

int f, e=f=3;

// declaration of b1 is missing in the above inst

// shell scripts



// " string - a sequence of characters "



// Compiler - (i) verifies whether the program is syntactically correct (ii) send instructions to the underlying Hardware to perfrom arithmetic

}

// /* comments  - multiple line comments   // single line comment */   // single line comment

// Internal Documentation - comments



// int a; a=3;  int a=3;

// a.out - executable file/binary file 

// Compiler - performs compilation - converts HLL (.c) - ML (a.out)

// can we have two different output files in the same directory

// int c; c=a+b;
// what if int d=4.1;  will it be stored as 4.1 or 4 or some other integer (some arbitrary value - junk)

// where can we see the output

// In IDE (Integrated Development Environment - a compiler, input screen, output screen, error screen along with some help for debugging )

// bug - error


// how do i interact with monitor/display screen -- how can a program interact with the display screen




// can we rename a.out

// error in the code  -- error on execution - NO a.out is created


// Types of errors  -  Syntax error , Semantic error (logical error /run-time error)

// sytax errors - compilation erros - you get to on running gcc

// semantic error/ run-time error - you get to see when you execute run ./a.out

// gcc filename works fine  run ./a.out -- systems throws an error -- run-time error





